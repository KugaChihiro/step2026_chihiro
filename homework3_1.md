## 宿題3-1

- 掛け算・割り算の処理（evaluate_multiplication_division）　→　足し算・引き算の処理（evaluate_addition_subtraction）　の二段階で処理を行う
- 掛け算・割り算の処理をし終えたリスト（*/の記号が残っていない状態）を、evaluate_addition_subtractionに渡すことで、上記の二段階での処理を実現

### 掛け算・割り算の処理

- 既存のリスト(tokens)を先頭から順番に走査する

- トークンの種類に応じて、以下の通り処理を行う

  - 通常の数字や演算子（+, -）の場合：
    - リスト（tokens）は変更せず、 index += 1 で次の要素へ進める

  - *, / の場合：
    - 演算子の前後（index - 1番目とindex + 1番目）の数字を使って、掛け算か割り算を行う
    - 計算に使った3つの要素（index - 1番目からindex + 1番目まで）をリストから削除する
    - 削除した位置（index - 1番目）に、計算結果から作った新しい数字のトークンを挿入する
    - ※新しく入った計算結果の次（= 現在のindexの位置）には、まだチェックしていない次の要素がスライドしてきている
        そのため、 index += 1 は行わずにその位置からチェックを続ける