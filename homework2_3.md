## 【第2回】宿題3

Q
キャッシュの管理をほぼ O(1) で実現できるデータ構造を考える

A
ハッシュテーブルと双方向連結リスト（Doubly Linked List）を組み合わせたデータ構造（一般にLRUキャッシュと呼ばれる）を採用。

既存データへの再アクセス時：
ハッシュテーブルを用いて対象ノードを O(1) で検索し、双方向連結リストのポインタ（prev, next）をつなぎ替えて、対象データを先頭へ O(1)で移動する。

最古データの削除（キャッシュ溢れ時）：
リストの末尾を指す"lastnode"を常に保持しておく。
これにより、前から順番に探索することなく、一番古いデータを O(1)で特定してリストから切り離すことができる。